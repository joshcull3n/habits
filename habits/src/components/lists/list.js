// List Object
// id, title, createdDate, editedDate

const List = ({ list }) => {
    return (
        <div className='tr'>
            <div className="td">{ list.title }</div>
            <div className="td"> bloop </div>
        </div>
    )
}

export default List;